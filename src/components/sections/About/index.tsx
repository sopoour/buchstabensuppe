import Section from '@app/components/layout/Section';
import { FC } from 'react';

const About: FC = () => {
  return (
    <Section id="über-uns">
      <p>new section</p>
    </Section>
  );
};

export default About;
